REACT_APP_USABET_API_BASE_URL=https://usabet9.com/api/v1

# Required by some deployments for /user/userLogin
# Example value from your working app:
REACT_APP_USABET_BASIC_AUTH="Basic YXBwbGljYXRpb246c2VjcmV0"

# DEV note:
# In `vite dev`, the app uses baseURL "/api" automatically (see `src/api-services/usabet-api/index.ts`)
# and `vite.config.ts` proxies it to https://usabet9.com/api/v1 to avoid CORS.

# Optional: override the app's generic REST API base too (used by `src/api/index.ts`)
REACT_APP_REST_API_URL=https://usabet9.com/api/v1


